{
  "name": "badclock",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && scp -r dist/* admin@kiosk.local:~/clock/ && ssh admin@kiosk.local 'sudo systemctl restart clock-server'"
  },
  "devDependencies": {
    "mediabunny": "^1.50.8",
    "vite": "^6",
    "vite-plugin-singlefile": "^2"
  },
  "dependencies": {
    "planck": "^1.4.2"
  },
  "version": "1.0.0"
}
